#
# template file for environment variables for hello app. Copy to .env and
# modify there. If .env does not exist, this file is used (in scripts; docker-compose
# will only read .env)
#

#port used by hello both in the app, and mapped to the host.
#HELLO_PORT=3000
#
